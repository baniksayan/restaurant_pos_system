import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors - Restaurant theme (Indigo/Slate premium aesthetic)
  static const Color primary = Color(0xFF6366F1);      // Modern Indigo
  static const Color primaryDark = Color(0xFF4F46E5);  // Deep Indigo
  static const Color primaryLight = Color(0xFF818CF8); // Soft Indigo
  static const Color accent = Color(0xFFF97316);       // Warm Orange

  // Background gradient colors
  static const Color backgroundStart = Color(0xFFF1F5F9); // Light Slate
  static const Color backgroundEnd = Color(0xFFF8FAFC);   // Pure Slate

  // Card and surface colors
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFF8FAFC);
  static const Color cardShadow = Color(0x0A0F172A); // Ultra-soft slate shadow

  // Status colors
  static const Color success = Color(0xFF10B981); // Emerald
  static const Color error = Color(0xFFEF4444);   // Rose/Red
  static const Color warning = Color(0xFFF59E0B); // Amber
  static const Color info = Color(0xFF0EA5E9);    // Sky Blue

  // Text colors
  static const Color textPrimary = Color(0xFF0F172A);   // Slate 900
  static const Color textSecondary = Color(0xFF475569); // Slate 600
  static const Color textHint = Color(0xFF94A3B8);      // Slate 400
  static const Color textOnDark = Color(0xFFFFFFFF);

  // Table status colors (modern, soft tones)
  static const Color tableAvailable = Color(0xFF10B981);
  static const Color tableOccupied = Color(0xFFEF4444);
  static const Color tableReserved = Color(0xFFF59E0B);
  static const Color tableCleaning = Color(0xFF6366F1);
  
  // KOT related status color (purple accent)
  static const Color kotStatus = Color(0xFF8B5CF6);
}

